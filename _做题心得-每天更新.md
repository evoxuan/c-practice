
## 9.8
做题之前一定要分析题。今天那道彩票的题，我完全没有搞清楚逻辑，就准备按照固有逻辑去写，实际不能是这样


可以，给你压成适合你之后复习的“错因 + 思维”笔记。

## 今日两题做题笔记

### 1. 彩票中奖统计

**核心思路：**
不要“按奖项分别判断”，而要：

> 对每一张彩票，先统计它匹配了几个号码，再给对应奖项计数。

结构：

```cpp
for (每张彩票) {
    int cnt = 0;

    for (彩票的7个数)
        for (中奖的7个数)
            if (相等)
                cnt++;

    c[cnt]++;
}
```

**易错点：**

* `cnt` 是“当前这一张彩票”的匹配数，所以每张彩票开始时要重新 `cnt = 0`
* `c[cnt]++` 要在这一整张彩票判断完之后执行，不能每比完一个号码就加
* 奖项与匹配数的对应：

  * 7 个 → 特等奖
  * 6 个 → 一等奖
  * ...
  * 1 个 → 六等奖
* 因此可以直接用：

```cpp
c[cnt]++;
```

最后倒序输出 `c[7] ~ c[1]`

**我这题真正卡住的地方：**

> 没先搞清楚“统计单位是什么”。

统计单位是“一张彩票”，不是“一个匹配数字”，也不是“一个奖项”。

---

### 2. 奇数阶幻方

**核心思路：**
严格按题目分 4 种情况写：

```cpp
if (...)
else if (...)
else if (...)
else ...
```

每放一个新数字，只能进入一种情况。

**易错点：**

* 赋值和比较别混：

```cpp
a[x][y] = 1;   // 赋值
a[x][y] == 1;  // 比较
```

* “右上”在二维数组坐标里是：

```cpp
x - 1
y + 1
```

* “正下方”是：

```cpp
x + 1
y
```

* 循环要包含 `N*N`：

```cpp
for (int i = 2; i <= N*N; i++)
```

不能写成 `< N*N`

* 连续多个独立 `if` 可能导致：

  * 前一个 `if` 修改了 `x,y`
  * 后一个 `if` 又成立
  * 同一个数字被放多次

所以互斥情况用：

```cpp
if
else if
else if
else
```

---

### 3. 今天最值得记住的两个编程习惯

**第一：先确定“一个循环到底在处理谁”。**

比如彩票题：

```text
外层循环 = 一张彩票
```

变量属于谁，就放在哪一层。

**第二：判断数组越界不能只看某一行代码。**

例如：

```cpp
a[x-1][y+1]
```

看起来危险，但如果前面的条件已经保证：

```text
x >= 2
y <= N-1
```

那它其实不会越界。

所以要学会：

> **结合前面的条件判断坐标是否合法。**

你今天这两题本质上一个是在练**统计对象与循环层次**，另一个是在练**条件分支与二维坐标变化**。这两个都很基础，但非常重要。
